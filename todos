Rerun on core:
- Prioritize lack of segv

For GC on shapes:
- When an object is marked, also mark its shape
- When a shape is marked, it marks its parent
- Remove four fields so shape fits as an IMEMO object in 40 bytes

Get memory profile data:
- run "/usr/bin/time -l" on disourse, or railsbench or something for comparative
  memory_footprint

preallocate shapes
